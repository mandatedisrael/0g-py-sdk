[pytest]
# Register custom markers
markers =
    integration: marks tests as integration tests that require testnet access (deselect with -m "not integration")
    slow: marks tests as slow (e.g. waiting for finalization), skipped by default
